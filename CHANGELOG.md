# Changelog

## [Unreleased]

### Added

- Added the `report` command with a `--output` flag that accepts `table` (default) or `json`

All notable changes to this project are documented here.

## [1.0.0]

### Added

- Initial release with the `report` command
