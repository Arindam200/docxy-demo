# Changelog

All notable changes to this project are documented here.

## [Unreleased]

### Added

- Added the `report` command with an `--output` flag supporting `table` (default) and `json` formats

## [1.0.0]

### Added

- Initial release with the `report` command
